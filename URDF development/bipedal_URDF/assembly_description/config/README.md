# RViz Configuration (`assembly_description/config`)

Contains RViz configuration profiles for visualizing the complete robot assembly model.

## Files
- **`display.rviz`**: RViz workspace profile configured with `RobotModel`, `Grid`, and `TF` displays.
