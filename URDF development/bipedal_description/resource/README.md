# ROS 2 Ament Resource Index (`bipedal_description/resource`)

Contains empty marker file `bipedal_description` required by ROS 2 `ament` build system to register the package in the environment resource index for package discovery (`ros2 pkg list`, `get_package_share_directory`).
