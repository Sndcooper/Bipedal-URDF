# Configuration Profiles (`bipedal_description/config`)

This folder contains pre-configured RViz 2 visualization workspace profiles for displaying the robot model, joint frames, TF trees, and link meshes.

## Files
- **`display.rviz`**: Standard RViz profile loaded by `display.launch.py`. Pre-configured with Grid, RobotModel, TF, and Joint State Publisher displays.
