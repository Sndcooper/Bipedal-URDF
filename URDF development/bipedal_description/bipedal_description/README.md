# Python Package Module Marker (`bipedal_description`)

This directory serves as the Python module root for the `Bipedal_description` ROS 2 `ament_python` package.

## Files
- `__init__.py`: Package initialization script required by Python build systems and ROS 2 `setup.py`.
